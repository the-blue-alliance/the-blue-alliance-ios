//
//  TBAAPI+Match.swift
//  TBAAPI
//
//  Created by Zachary Orr on 8/19/24.
//

import TBAAPI

/*
extension TBAAPI {
    public func getMatch(matchKey: MatchKey) async throws -> [Match] {
        let response = try await client.getMatch(path: .init(match_key: matchKey))
        return try convertResponse(response: response.ok.body.json)
    }

    public func getMatchZebraMotionworks(matchKey: MatchKey) async throws -> [MatchZebra] {
        let response = try await client.getMatchZebra(path: .init(match_key: matchKey))
        return try convertResponse(response: response.ok.body.json)
    }
}
*/
