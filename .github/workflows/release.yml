name: Release

on:
  workflow_dispatch:
    inputs:
      lane:
        description: "What to build"
        required: true
        type: choice
        options:
          - TestFlight
          - App Store
        default: TestFlight
      confirm:
        description: "Retype the selection (TestFlight or App Store) to confirm"
        required: true
        type: string

jobs:
  release:
    name: Release (${{ inputs.lane }})
    runs-on: macos-latest
    if: ${{ inputs.confirm == inputs.lane && github.ref == 'refs/heads/main' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Read Xcode version
        id: xcode
        run: echo "version=$(cat .xcode-version)" >> "$GITHUB_OUTPUT"
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ steps.xcode.outputs.version }}
      - uses: actions/cache@v6
        with:
          path: vendor/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: ${{ runner.os }}-gems-
      - name: Install
        run: |
          cp mock-Secrets.plist the-blue-alliance-ios/Secrets.plist
          make secrets
          # Without this the vendor/bundle cache above never gets a hit.
          bundle config set --local path vendor/bundle
          bundle install
          bundle exec fastlane run setup_ci
        env:
          TBA_API_KEY: ${{ secrets.TBA_API_KEY }}
      - name: fastlane beta_ci
        if: ${{ inputs.lane == 'TestFlight' }}
        run: bundle exec fastlane beta_ci
        env:
          APPLE_KEY_ID: ${{ secrets.APPLE_KEY_ID }}
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APPLE_KEY_CONTENT: ${{ secrets.APPLE_KEY_CONTENT }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          MATCH_GIT_REPO: ${{ secrets.MATCH_GIT_REPO }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          SLACK_URL: ${{ secrets.SLACK_URL }}
      - name: fastlane app_store
        if: ${{ inputs.lane == 'App Store' }}
        run: bundle exec fastlane app_store
        env:
          APPLE_KEY_ID: ${{ secrets.APPLE_KEY_ID }}
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APPLE_KEY_CONTENT: ${{ secrets.APPLE_KEY_CONTENT }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          MATCH_GIT_REPO: ${{ secrets.MATCH_GIT_REPO }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          SLACK_URL: ${{ secrets.SLACK_URL }}
      - name: Archive build logs
        if: ${{ failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: build-logs-${{ inputs.lane == 'TestFlight' && 'testflight' || 'appstore' }}-${{ github.run_number }}
          path: logs/
          if-no-files-found: warn
          retention-days: 30
      - name: Archive dSYMs
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: dsyms-${{ inputs.lane == 'TestFlight' && 'testflight' || 'appstore' }}-${{ github.run_number }}
          path: |
            **/*.dSYM.zip
            **/*.dSYM
          if-no-files-found: warn
          retention-days: 90
